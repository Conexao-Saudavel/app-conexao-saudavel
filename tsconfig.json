{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // Necessário para paths
    "paths": {
      "@/*": ["src/*"] // Seu alias
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
  // ... outras configs
}