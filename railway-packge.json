{
  "name": "conexao-saudavel-frontend",
  "version": "1.0.0",
  "description": "Frontend do aplicativo Conexão Saudável",
  "scripts": {
    "start": "serve -s dist -l ${PORT:-8080}",
    "build": "bash ./build.sh",
    "web:build": "cd mobile && npm run web:build"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}